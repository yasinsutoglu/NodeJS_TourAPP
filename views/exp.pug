doctype html 
html 
    head
        title TourApp | #{tour} 
        link(rel='stylesheet' href='css/style.css')
        link(rel='shortcut icon' type='image/png' href='img/favicon.png')

    body
        h1= tour
        h2= user.toUpperCase()
        //- h1 The Park Camper
        - const x = 9;
        h2= 2 * x
        p This is text

         //- "=" sonrası JS kullanılabilir. "-" sonrası direkt JS code'ları yazılabilir. #{} -> interpolation syntax'tır.